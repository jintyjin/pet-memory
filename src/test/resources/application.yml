spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/petmemory;
    username: jin
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

  messages:
    basename: messages, errors

logging:
  level:
    org.hibernate.SQL: debug

file:
  dir: /Users/jinsejin/Desktop/files/images/